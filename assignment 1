#include<stdio.h>
main()
{
    int n,i,r,count=0,s,e;
    scanf("%d%d",&s,&e);
    for(n=s;n<=e;n++)
    {
        count=0;
        for(i=2;i<=n/2;i++)
        {
            r=n%i;
            if(r==0)
            {
                count++;
            }
        }
        if(count==0)printf("%d",n)
    }
}
